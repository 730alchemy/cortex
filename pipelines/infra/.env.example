# MinIO Configuration
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123
MINIO_ENDPOINT=http://localhost:9000
MINIO_CONSOLE_URL=http://localhost:9001

# PostgreSQL Configuration
POSTGRES_USER=cortex
POSTGRES_PASSWORD=cortex
POSTGRES_DB=catalog
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Neo4j Configuration
NEO4J_PASSWORD=cortexpassword
NEO4J_URI=bolt://localhost:7687
NEO4J_HTTP_URL=http://localhost:7474

# Qdrant Configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334

# Marquez Configuration
MARQUEZ_URL=http://localhost:5000
MARQUEZ_API_URL=http://localhost:5001

# Dagster Configuration
DAGSTER_HOME=/opt/dagster/dagster_home
DAGSTER_POSTGRES_HOST=postgres
DAGSTER_POSTGRES_PORT=5432
DAGSTER_POSTGRES_DB=dagster
DAGSTER_POSTGRES_USER=cortex
DAGSTER_POSTGRES_PASSWORD=cortex

# Dagster Workspace Configuration
# Options: workspace.yaml (default), workspace.docker.yaml, workspace.local.yaml, workspace.hybrid.yaml
# DAGSTER_WORKSPACE_FILE=workspace.yaml

# Pipeline Configuration
WATCH_DIRECTORY=/watch/file_drop
INGEST_INTERVAL_SECONDS=30
DATA_LAKE_BUCKET=data-lake
WAREHOUSE_BUCKET=warehouse
