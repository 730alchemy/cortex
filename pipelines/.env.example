# Environment Variables Configuration
#
# Copy this file to .env and fill in your values
# cp .env.example .env

# INPUT_DIR - Directory path for the sensor to monitor for new files
# The file monitoring sensor will detect new files added to this directory
# and trigger asset materialization automatically.
# Example: INPUT_DIR=/path/to/your/input/directory
INPUT_DIR=
